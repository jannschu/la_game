/**
 * Controls the whole game
 * @param {LaGameGUI} gui The gui the game is displayed
 * @param {LaGamePlayer} playerA input source for player one (or A)
 * @param {LaGamePlayer} playerB input source for player two (or B)
 */
function LaGameLogic(gui, playerA, playerB) {
  
};

/**
 * Lets the input sources choose a source
 * @param {Boolean} neutral Can he/she/it move a Neutral-Piece?
 * @param {Boolean} l Can he/she/it move his/her/its L-Piece?
 * @param {Number} player The player: 1 or 2
 * @returns the new game board object
 */
LaGameLogic.prototype.getMove = function(neutral, l, player) {
  
};